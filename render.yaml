services:
  - type: web
    name: farm-manager-api
    env: node
    buildCommand: cd server && npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
